# Rankings folder

Place ranking CSV files in this folder. The build automatically reads every CSV file, validates the required columns, and creates ranking pages.

Use `content/templates/ranking_template.csv` as the starting point. The first row of each file supplies the shared page information such as title, sport, gender, division, season, slug, and updated date. Repeat those shared values on every row when exporting from Google Sheets.
